{
  "name": "contxt-domain",
  "version": "0.1.0",
  "private": true,
  "description": "Cœur métier de Contxt : modèles unifiés et moteur de suggestion contextuelle. TypeScript pur, sans dépendance UI, entièrement testé.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
